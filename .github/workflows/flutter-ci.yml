name: Full Flutter CI

on:
  # ESSystem: apenas disparo manual (gatilhos push/pull_request desativados
  # para nao rodar a matriz completa a cada push no master).
  workflow_dispatch:

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
